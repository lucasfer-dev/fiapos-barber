import Link from 'next/link'; import {ArrowRight} from 'lucide-react'; import {PublicLayout} from '@/components/PublicLayout'; import {haircutCatalog} from '@/lib/demo-data';
export default function Page(){return <PublicLayout><main className="section section-dark-alt"><div className="container"><div className="page-hero" style={{borderColor:'#292b2e'}}><div><span className="eyebrow">Inspire-se</span><h1 style={{color:'white'}}>Encontre uma referência para o seu próximo corte.</h1><p style={{color:'#a8aaac'}}>Use o catálogo como inspiração e leve a referência direto para o agendamento.</p></div></div><div className="look-grid" style={{marginTop:30}}>{haircutCatalog.map((c,i)=><article className={`look-card look-${i+1}`} key={c.id}><div className="look-shade"/><div className="look-content"><span>{c.category}</span><h3>{c.name}</h3><p>{c.description}</p><Link href={`/agendar?haircut=${c.id}`} className="look-link">Quero esse corte <ArrowRight size={16}/></Link></div></article>)}</div></div></main></PublicLayout>}
