import Link from 'next/link'; import {ArrowRight,Scissors} from 'lucide-react'; import {PublicLayout} from '@/components/PublicLayout'; import {barbers} from '@/lib/demo-data';
export default function Page(){return <PublicLayout><main className="section section-light"><div className="container"><div className="page-hero"><div><span className="eyebrow dark">Nossa equipe</span><h1>Escolha quem vai cuidar do seu visual.</h1><p>Ao selecionar vários serviços, o sistema filtra automaticamente os profissionais capazes de realizar a combinação completa.</p></div></div><div className="grid">{barbers.map(b=><article className="card" key={b.id}><div className="catalog-thumb"><Scissors/><strong style={{marginLeft:8}}>Foto oficial a cadastrar</strong></div><h3 style={{fontFamily:'Georgia,serif',fontSize:26,fontWeight:500}}>{b.name}</h3><p className="muted" style={{fontSize:12,lineHeight:1.7}}>{b.bio}</p><div className="category-tabs" style={{padding:'8px 0 18px'}}>{b.specialties.map(x=><span className="service-category" key={x}>{x}</span>)}</div><Link className="btn btn-primary" href={`/agendar?barber=${b.id}`}>Agendar com este profissional <ArrowRight size={16}/></Link></article>)}</div></div></main></PublicLayout>}
