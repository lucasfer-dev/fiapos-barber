'use client';
import {useState} from 'react';
import {AdminShell} from '@/components/AdminShell';
import {useAdminStore} from '@/components/admin/useAdminStore';
const labels={pending:'Pendente',confirmed:'Confirmado',completed:'Concluído',cancelled:'Cancelado'} as const;
export default function Page(){const {appointments}=useAdminStore();const [date,setDate]=useState(new Date().toISOString().slice(0,10));const list=appointments.filter(a=>a.date===date&&a.status!=='cancelled').sort((a,b)=>a.time.localeCompare(b.time));return <AdminShell><div className="admin-heading"><div><span className="eyebrow dark">Agenda</span><h1>Agenda do dia</h1><p>Visualize a sequência de atendimentos e quem está responsável por cada cliente.</p></div><label className="month-picker"><input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label></div><section className="admin-card"><div className="agenda-timeline">{list.length?list.map(a=><div className="agenda-item" key={a.id}><div className="agenda-hour">{a.time}</div><div className="agenda-line"><span/></div><div className="agenda-content"><div><strong>{a.customer}</strong><small>{a.serviceNames.join(' + ')} • {a.duration} min</small></div><div><strong>{a.barberName}</strong><span className={`status-badge ${a.status}`}>{labels[a.status]}</span></div></div></div>):<div className="admin-empty">Nenhum atendimento nesta data.</div>}</div></section></AdminShell>}
